#include<stdio.h>
void main()
{
    int i,sum=0,n;
    printf("enter array size");
    scanf("%d",&n);
    int a[n];
    printf("enter array values:");
    for(i=0;i<n;i++)
    {
    scanf("%d",&a[i]);
    }
    for(i=0;i<n;i++)
    {
    sum=sum+a[i];
    }
    printf("sum=%d",sum);
}
