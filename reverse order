#include<stdio.h>
void main()
{
    int n,rem,rev=0;
    printf("enter a number:");
    scanf("%d",&n);
    while(n!=0)
    {
    rem=n%10;
    rev=rem+(rev*10);
    n=n/10;
    }
    printf("reverse order is %d",rev);
}
